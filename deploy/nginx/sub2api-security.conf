# 按真实 TCP 客户端地址限制连接和认证请求，避免信任客户端伪造的转发头。
limit_conn_zone $binary_remote_addr zone=sub2api_conn:20m;
limit_req_zone $binary_remote_addr zone=sub2api_auth:20m rate=2r/s;

# 仅在客户端请求 WebSocket 升级时转发 upgrade 连接头。
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
